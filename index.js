const bodyParser=require("body-parser"),express=require("express"),cors=require("cors");require("dotenv").config();const PORT=process.env.PORT,routes=require("./routes/routes.js"),app=express();app.use(bodyParser.urlencoded({extended:!1})),app.use(bodyParser.json()),app.use(cors()),app.use("/",routes),app.listen(PORT,()=>{console.log(`Server started at http://localhost:${PORT}`)});