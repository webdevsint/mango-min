const documents=require("../documents/list"),path=require("path"),fs=require("fs"),document=process.argv[2];if(document){let e=path.resolve(__dirname,"../documents",document);if(documents.includes(document)){let t=documents.indexOf(document);-1!==t&&documents.splice(t,1),fs.rmSync(e,{recursive:!0}),fs.writeFileSync(path.resolve(__dirname,"../documents/list.js"),`const documents=${JSON.stringify(documents)};module.exports=documents;`),console.log(`Document "${document}" deleted!`)}else console.log("Oops! This document doesn't exist!")}else console.log("Please provide a document name!");